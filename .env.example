# ------------------------------------------------------------
# 🌍 Application Environment Configuration
# ------------------------------------------------------------

# The current environment (development, staging, production)
NODE_ENV=development

# The human-readable application name
APP_NAME="Collab Blog"

# ------------------------------------------------------------
# 🧠 API Configuration
# ------------------------------------------------------------

# Base URL for your backend API (used for all HTTP requests)
# Example: http://localhost:5000/api/v1 or https://api.yourdomain.com/v1
API_URL=<api_url>

# Base server URL (without /api prefix)
# Example: http://localhost:5000 or https://yourdomain.com
BASE_URL=<base_url>

# WebSocket server URL for real-time updates
# Example: ws://localhost:5000 or wss://yourdomain.com
SOCKET_URL=<websocket_url>

# ------------------------------------------------------------
# 🔐 Authentication & Tokens
# ------------------------------------------------------------

# LocalStorage key for storing access token
TOKEN_KEY=<toke_name_key>

# LocalStorage key for storing refresh token
REFRESH_TOKEN_KEY=<refresh_toke_name_key>

# LocalStorage key for storing token expiration time
TOKEN_EXPIRATION_KEY=<expiration_toke_name_key>

# Whitelisted domains for JWT (comma-separated, no spaces)
# Example: localhost:5000,api.yourdomain.com
JWT_WHITELISTED_DOMAINS=<cors_domain>

# Blacklisted routes for JWT (comma-separated, no spaces)
# Example: /auth/login,/auth/register
JWT_BLACKLISTED_ROUTES=

# ------------------------------------------------------------
# ✅ Production Flag
# ------------------------------------------------------------

# Set to "true" for production, "false" for development
PRODUCTION=<boolean>
